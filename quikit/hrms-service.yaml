apiVersion: v1
kind: Service
metadata:
  name: hrms-service
  namespace: quikit
  
spec:
  selector:
    app: hrms
  ports:
    - protocol: TCP
      port: 3009
      targetPort: 3009
  type: NodePort