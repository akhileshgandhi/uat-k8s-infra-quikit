apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: quikit
  annotations:
    cloud.google.com/backend-config: '{"default": "quikit-backend-config"}'
spec:
  selector:
    app: crm
  ports:
    - protocol: TCP
      port: 3008
      targetPort: 3008
  type: NodePort