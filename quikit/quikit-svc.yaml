apiVersion: v1
kind: Service
metadata:
  name: quikit-service
  namespace: quikit
  annotations:
    cloud.google.com/backend-config: '{"default": "quikit-backend-config"}'
spec:
  selector:
    app: quikit
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort