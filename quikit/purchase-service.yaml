apiVersion: v1
kind: Service
metadata:
  name: purchase-service
  namespace: quikit
spec:
  selector:
    app: purchase
  ports:
  - protocol: TCP
    port: 3020
    targetPort: 3020
  type: NodePort
