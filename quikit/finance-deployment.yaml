apiVersion: apps/v1
kind: Deployment
metadata:
  name: quikfinance-deployment
  namespace: quikit
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: quikfinance
  template:
    metadata:
      labels:
        app: quikfinance
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "global-role"
        vault.hashicorp.com/agent-init-first: "true"
        vault.hashicorp.com/agent-inject-secret-config: "secret/data/quikit/auth-service"
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/quikit/auth-service" -}}
          {{- range $k, $v := .Data.data }}
          export {{ $k }}="{{ $v }}"
          {{- end }}
          {{- end }}

    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
      - name: quikfinance
        image: ghcr.io/akhileshgandhi/quikfinance-runtime:sha-89731ca

        command:
        - /bin/sh
        - -c
        - |
          set -a
          . /vault/secrets/config
          set +a
          cd /app/apps/quikfinance
          exec node server.js

        ports:
        - containerPort: 3013

        envFrom:
        - configMapRef:
            name: quikit-config
        
        env:
          - name: NEXTAUTH_URL
            value: "https://uatquikfinance.quikit.ai"
          - name: QUIKIT_CLIENT_ID
            value: "quikfinance"

        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "256Mi"

        livenessProbe:
          httpGet:
            path: /api/health
            port: 3013
          initialDelaySeconds: 45
          periodSeconds: 20

        readinessProbe:
          httpGet:
            path: /api/health
            port: 3013
          initialDelaySeconds: 15
          periodSeconds: 10
